# Backups
*.bak-*
*.backup

# Logs / cache
*.log
.DS_Store

# Secrets — SSH (NE JAMAIS COMMIT)
id_ed25519
id_ed25519.pub
*.pem
*.key

# Claude credentials (NE JAMAIS COMMIT)
.credentials.json
auth-status-cache.json
usage-cache.json
statusline-last-input.json

# Données perso Claude (sessions, history)
sessions/
history.jsonl
projects/
stats-cache.json

# Config Claude Code locale au repo (settings par machine, permissions, etc.)
.claude/

# Archive locale (code mort gardé pour historique, pas dans le repo public)
.archive/

# Hook local-only : patche claude.exe pour lever le clamp refreshInterval.
# Détecté FileFix.BBA!MTB par Defender → ne JAMAIS push sur le repo public.
# Cf. commit a674338. Defense layer 2 (layer 1 = blacklist dans deploy.ps1).
claude-code/hooks/patch-claude-exe.ps1
