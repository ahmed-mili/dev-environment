# Backups
*.bak-*
*.backup

# Logs / cache
*.log
.DS_Store
.codex-remote-attachments/

# Secrets — SSH (NE JAMAIS COMMIT)
id_ed25519
id_ed25519.pub
*.pem
*.key

# Claude credentials (NE JAMAIS COMMIT)
.credentials.json
auth-status-cache.json
usage-cache.json
statusline-last-input.json

# Données perso Claude (sessions, history)
sessions/
history.jsonl
projects/
stats-cache.json

# Config Claude Code locale au repo (settings par machine, permissions, etc.)
.claude/

# Archive locale (code mort gardé pour historique, pas dans le repo public)
.archive/

# Plans / docs internes d'exécution (workflow perso, pas pour le repo partageable)
docs/
