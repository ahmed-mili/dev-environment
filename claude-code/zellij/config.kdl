// Zellij — config partagée (projets WSL & vaults Windows).
// Cf. docs/superpowers/plans/2026-06-02-zellij-everywhere.md
//
// Zellij fusionne ce fichier avec ses défauts : on ne met QUE les overrides.
// Déploiement :
//   WSL     -> ~/.config/zellij/config.kdl
//   Windows -> %APPDATA%\zellij\config\config.kdl  (retirer copy_command, cf. note)

theme "tokyo-night"          // proche de ton thème tmux Tokyo Night
mouse_mode true              // scroll molette + clic panes/tabs
scroll_buffer_size 50000     // gros scrollback (comme ton history-limit tmux)
pane_frames false            // épure, comme ton tmux minimaliste
session_serialization false  // pas de résurrection au reboot → jamais d'écran "Waiting to run"

// WINDOWS uniquement : sans ça, Zellij ouvre le pane dans cmd.exe (shell Windows
// par défaut). On force PowerShell 7. NE PAS mettre cette ligne côté WSL (bash OK).
// ⚠ DEUX pièges (chacun donne "program not found" → pane "Waiting to run" → écran vide) :
//   1. `default_shell "pwsh"` échoue : pwsh est un App Execution Alias (stub
//      WindowsApps 2 octets) que Zellij ne suit pas.
//   2. Un chemin en FORWARD-SLASHES échoue AUSSI : CreateProcess (Windows) ne
//      résout pas "C:/Program Files/.../pwsh.exe" (slash + espace). Il faut des
//      BACKSLASHES échappés. Testé : seul ce format spawn pwsh 7.
//   default_shell "C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_<VER>_x64__8wekyb3d8bbwe\\pwsh.exe"
// Le <VER> change à chaque update de PowerShell → re-résoudre en pwsh avec :
//   (Get-Command pwsh).Source     # puis remettre le chemin (en \\) dans %APPDATA%\zellij\config\config.kdl

// copy_on_select : true par DÉFAUT chez Zellij -> la copie-à-la-sélection que tu aimes.
// Copie : OSC52 par défaut (validé au POC, marche via SSH/tél) -> PAS de copy_command,
//   sinon Zellij copierait en local au lieu de transmettre au terminal client (le tél).
// On garde la barre de hints Zellij (tu l'aimes) -> pas de simplified_ui.
